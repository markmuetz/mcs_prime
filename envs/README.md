# MCS:PRIME conda envs

conda environments used for running all MCS:PRIME code, and scripts for creating/updating them.
`conda_env` must be used (or equivalent) to get this code working.

    conda activate conda_env
